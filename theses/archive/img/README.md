# Thesis cover images

Drop the cover images referenced in `_data/geotheses.yml` here, keeping the
filenames exactly as in the YAML (e.g. `2026_Aalders.jpg`). The archive page
picks them up at the next build: it checks `site.static_files`, so entries
whose file is absent show a placeholder instead of a broken image.
